<table class="calendar">
    <tr><td colspan="{{ calendar.widthRenderedCalendarInDays }}" id="calendar_month">{{ calendar.renderMonthName }}</td></tr>
    <tr>
        {%  for day in days %}
            <th>{{ day }}</th>
        {%  endfor %}
        {{ calendar.renderWeekDays }}
    </tr>
    <tr>{{ calendar.renderDates }}</tr>
    {{ calendar.renderMonth }}
</table>